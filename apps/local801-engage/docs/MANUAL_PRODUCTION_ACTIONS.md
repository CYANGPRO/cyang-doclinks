# Manual production actions

This is the owner checklist for work that cannot be completed from the repository alone. Do not paste secret values into GitHub issues, commits, screenshots, chat, or runbook evidence. Record only safe resource IDs, dates, results, and change-ticket references.

Production and real member data remain locked until every launch-blocking item is complete and Chang Yang gives the final written go-live approval.

## Accounts and governance

- [x] Protect `preview` with a pull-request-only branch rule. Direct push was rejected as expected on 2026-08-19, and PR #38 passed the repository validation workflow before merge.
- [x] Require the release gate and Vercel deployment check before merging the production promotion PR. Verified on 2026-08-19: the active `Protect Main` ruleset requires a pull request, strict up-to-date `validate` and `Vercel` status checks, blocks force pushes/deletion, and has no bypass actor.
- [x] Supersede the stale default-branch tree by promoting the accepted audited `preview` head through PR #40. `main` CI passed the full validation workflow on 2026-08-19, including a zero-vulnerability Production dependency audit.
- [x] Sheng Yang completed the independent authorization/security review and penetration test for commit `48e801a4cfffa95954061486961660799b7c14b0` on 2026-08-19 at 9:00 p.m. (owner-recorded time). Review `L801-SEC-2026-08-19-SY-01` approved Production with no findings or conditions; the external penetration-test report reference is `cat.chang.io-TESTANDREVIEWv1`. The report and reviewer contact details remain outside the repository.
- [ ] Obtain counsel/organizational approval for the privacy, retention/deletion, incident, vendor, and access-control procedures.
- [x] Name pilot participants, the support contact, incident commander, database restore operator, and credential/key custodians. Chang Yang holds the primary owner-operated roles; Sheng Yang is the named backup technical maintainer and backup credential/key custodian. Contact details remain in the owner-controlled operations record, not this repository.

## Dedicated production resources

- [x] In Vercel, configure `cyang-cat-data` for Node 24, the approved Cleveland execution region, Production-only environment variables, Deployment Protection for non-production deployments, and a USD 300/month budget alert. Node 24, isolated Production variables, `cle1` function execution, Git-fork protection, authentication for non-custom-domain deployments, and the USD 300 on-demand threshold with notifications on and automatic pausing off were verified on 2026-08-19. Controlled deployment `dpl_7tC4NriQremiPWkvyMHPQTfWQ89G` remained launch-disabled and error-free.
- [x] Connect the Vercel project to the canonical private GitHub repository and track `main` for Production. The stale `cyangprojects-sys` link was replaced with `CYANGPRO/cyang-cat-data` on 2026-08-19 after PR #40 promoted the accepted feature-complete tree to `main`; the next reviewed evidence PR is the automatic-deployment acceptance test.
- [x] Use the existing isolated `cyang-cat-data` Production project/database in AWS Virginia under the owner-approved 2026-08-19 region exception. It is separate from DocLinks.
- [x] Upgrade the Neon organization from Free to Launch and protect the `production` branch before applying the Production schema. Completed 2026-08-19; Production branch `br-summer-river-awzfl7hl` is protected.
- [x] Configure the Neon Production history window to seven days and prove point-in-time recovery on a disposable target. Completed 2026-08-19: a branch at 9:55 a.m. CDT was forked in 0.29 seconds, the recovered schema/bootstrap state was validated in 58 seconds, and the disposable branch was deleted. This demonstrated a five-minute recovery point and remained inside the approved one-hour RPO/four-hour RTO.
- [x] In Cloudflare R2, create the private Production bucket in `enam`, disable public access and `r2.dev`, and issue a least-privilege CAT-only token. The isolated `local801-engage-private` bucket, Eastern North America (`ENAM`) placement, disabled public access, disabled Public Development URL, no custom domains, bucket-scoped Object Read & Write token and Production-only Vercel credentials were verified on 2026-08-19. The guarded live drill then wrote one application-encrypted synthetic object, authenticated its download/decryption, deleted it, and verified absence; `storageRecovery`, `encryptedRoundTrip`, and `cleanup` all returned `ok`.
- [x] Configure the owner VPS scanner at `https://scan.cyang.io` with a Production-specific client ID/HMAC secret, TLS, request limits, no-retention behavior, and clean/infected/unavailable acceptance paths. Completed 2026-08-19: the separate CAT adapter runs as an unprivileged systemd service on loopback-only `127.0.0.1:8089`, connects through the local ClamAV Unix socket, and is exposed only by the existing Caddy `POST /v1/scan` handler. The Production-only client ID and HMAC secret are escrowed and stored as Sensitive Vercel variables. Guarded synthetic acceptance returned `ok` for clean, infected, unavailable/fail-closed and post-restart recovery paths; deployment `dpl_58FR7QPFmmjZXHD3n3czsNKpr1PF` reached READY at `cat.cyang.io` with launch and real-data flags still disabled.
- [x] Create the Sentry Production project, configure a CAT-only DSN, disable default PII collection, define redaction/scrubbing rules, and route alerts to the named owner/support channel. Production project `local801-cat-production` (safe project ID `4511938528804864`) and Sensitive Production-only `LOCAL801_SENTRY_DSN`/`LOCAL801_SENTRY_ENABLED` variables were verified on 2026-08-19 in READY deployment `dpl_AYVFwDEq7SH8F7d4T242LSkBfYCm` at main commit `9b327aa472710a602043bfa2101e75139b3d0733`. Server-side data scrubbing, default scrubbers and IP-address suppression were enabled. PII-free issue `LOCAL801-CAT-PRODUCTION-1` (`Local801SentryAcceptance: [redacted]`) was accepted and displayed, the enabled email alert recorded one trigger, and Chang Yang confirmed recipient-side delivery. No DSN or recipient address is recorded here.

## Identity, domain, and browser push

- [x] Register the Microsoft Entra ID application for `https://cat.cyang.io/api/auth/callback/<provider-id>` and configure Production credentials plus the `email` and `verified_primary_email` ID-token claims. The exact callback, claims, Production client ID and escrowed Production client secret were verified on 2026-08-19. The application accepts a verified email only when `email_verified=true` or `verified_primary_email` exactly matches `email`.
- [x] Apply invitation-only assignment and MFA to every current pilot/production user. On 2026-08-19 the Enterprise Application was changed to require explicit assignment, remained hidden from My Apps, and Chang Yang was the only assigned user. Microsoft Security Defaults remain enabled for tenant-wide MFA; the correctly scoped `CAT - Require MFA` Conditional Access policy remains report-only because Entra will not enforce it unless Security Defaults are disabled. Self-service signup remains disabled.
- [ ] Enable the CAT-managed Entra onboarding flow in `docs/ENTRA_USER_ONBOARDING.md`. Owner confirmation and a non-secret Vercel listing verified the Graph consent/configuration step and all four Production onboarding variables on 2026-08-20. Migration 0025 was validated on temporary branch `br-tiny-moon-aw5k37of`, applied to protected Production branch `br-summer-river-awzfl7hl` as Neon migration `8ccac644-629f-4f31-868d-4606ed110058`, verified empty and structurally complete, and the temporary branch was deleted. The reviewed application commit, Production deployment, and one end-to-end invitation remain. After this cutover, do not manually maintain a second Entra user allowlist for ordinary CAT onboarding.
- [ ] Complete a live callback sign-in and verify the ID token carries an exact verified-primary-email match and `amr=mfa` before enabling production authentication.
- [x] Provision the initial protected Production organization and System Owner. Completed 2026-08-19 for organization slug `local801`: one encrypted/blind-indexed active System Owner, all seven role definitions, verified protected-only database state, one bootstrap audit event and zero member records.
- [x] Attach `cat.cyang.io` to the Vercel project and verify its external Cloudflare CNAME configuration. Vercel reported the domain correctly configured with no DNS issues on 2026-08-19.
- [ ] After the first accepted Production deployment, verify TLS, the Entra callback, canonical redirects, HSTS, and no indexing at `cat.cyang.io`. The launch-disabled 2026-08-19 deployment verified TLS, the canonical sign-in redirect, one-year HSTS, `X-Robots-Tag` protection, and a deny-all `robots.txt`; the live Entra callback remains pending while sign-in is intentionally locked.
- [ ] Generate owner-controlled VAPID keys and accept browser push on supported desktop/mobile browsers. Independent Production VAPID keys are configured; live browser acceptance remains pending.

### Deferred native publication (not an initial web/PWA launch blocker)

- [ ] In a separately approved post-launch native release, configure APNs/FCM, Apple Team ID, Android project number, app/site association files, and store signing identities. iOS archive/signing requires macOS/Xcode; Android signing requires the owner keystore and Android toolchain.
- [ ] In that native release, configure owner-controlled `attest.cyang.io` and `push.cyang.io` gateways with separate Production HMAC secrets, then accept device challenge/attestation, push registration, revocation, and generic-notification delivery.
- [x] Keep `LOCAL801_NATIVE_MOBILE_ENABLED=0` for the initial web/PWA release. Native store publication and physical-device acceptance are deferred by owner decision on 2026-08-19.

## Keys and secrets

- [x] Generate and configure independent Production values for `NEXTAUTH_SECRET`, object encryption, PII encryption, PII blind indexes, browser VAPID, and the CAT scanner HMAC. Completed 2026-08-19 as Sensitive Production-only Vercel variables; mobile gateway secrets remain pending with those owner-controlled services.
- [x] Store configured runtime secrets only in the Production environment and owner-controlled escrow/password manager. Chang Yang confirmed escrow on 2026-08-19; the temporary local key file was then removed and its absence verified. The scanner HMAC is separately escrowed; native-gateway secrets are intentionally deferred until those services are approved.
- [ ] Rehearse object-key and PII-key rotation using synthetic data; keep old versions until every object/row reconciles.
- [x] Configure all documented rate-limit values and `LOCAL801_PROTECTED_DURABLE_IMPORTS_ENABLED=1`; keep legacy Preview durable imports off. Verified in the isolated Production environment on 2026-08-19.

## Database and operations acceptance

- [x] Provide an isolated database named exactly `local801_sql_test` through `LOCAL801_SQL_TEST_DATABASE_URL`, verify it is disposable and contains no real data, then run `npm run test:sql-integration`. Accepted 2026-08-18 on the non-primary Neon development branch: migrations `0001`–`0024`, correction races, durable-job CAS, replay-safe 25K CSV, and real 20K service queries passed after the guarded schema reset.
- [x] Apply migrations `0001` through `0025` in order to the isolated, protected Production database under change control. Migrations `0001`–`0024` completed 2026-08-19 on branch `br-summer-river-awzfl7hl`; migration 0025 completed 2026-08-20 after temporary-branch validation and Production verification. The protected bootstrap remains limited to one organization, one encrypted System Owner and zero member records.
- [ ] Run the 25,000-row CSV/XLSX benchmark and scanner/Neon/R2/Workflow failure, retry, cancellation, browser-close, runless recovery, deployment-rollover, replay, concurrency, and reconciliation exercises. On 2026-08-19 the isolated Preview target passed private-R2 acceptance, clean scanner processing, a three-row durable Workflow completion, safe terminal failure persistence, cooperative cancellation of a running job, audited requeue, second-attempt completion, and persistence across deployments `dpl_AGU3YTMpMhZfRubdBiqemSNwz7RS` and its rollover successor. Protected Preview deployment `dpl_4XADbBjbkRx7hVjycXu9Xk5jCWuZ` then completed 25,000-row XLSX batch `bf3bb27a-fcfa-46ac-8cd6-8627c58cb3b5` on attempt 2 in Workflow run `wrun_01M0E1DF3CTQDAM7ZSS6VZA30X`, reaching `ready_for_review` after the upload client exited; the initial attempt correctly failed at the environment's former 10,000-row limit before that setting was aligned to the approved 25,000-row ceiling. The same deployment then recovered queued/runless three-row batch `31a80692-91e7-44a4-8d82-364568d1685e` through pinned run `wrun_01M0E1ZZXYQDVPM8TK0QQED1XV`; the attempt advanced from 1 to 2 and returned to `ready_for_review` with no error. Production scanner clean, exact standalone infected, unavailable/fail-closed and restart recovery paths also passed. The remaining dependency-outage/reconciliation exercises still block this combined item.
- [x] Run `LOCAL801_STORAGE_RECONCILIATION_CONFIRM=READ_ONLY npm run storage:reconcile`; investigate every missing/orphan/cleanup-pending count. Completed 2026-08-19 in read-only mode: 24 metadata objects matched 24 private bucket objects with zero missing, orphaned, cleanup-pending or duplicate records; `reconciled` was `true` and nothing was deleted.
- [x] Perform a Neon restore into the isolated restore target and an R2/object recovery exercise. The Neon portion passed on 2026-08-19 under operator Chang Yang: disposable branch `br-mute-brook-aw4qc0q7`, 9:55 a.m. CDT recovery point, 0.29-second fork, 58-second recovery/validation time, empty schema diff, 77 Local 801 tables, 24 reporting views, one protected System Owner, seven roles, zero member records, and cleanup confirmed. The guarded R2 exercise also passed: encrypted synthetic write/download/decrypt/delete/absence verification returned all `ok`, followed by read-only reconciliation of 24 metadata objects to 24 private bucket objects with zero missing, orphaned, cleanup-pending, or duplicate records.
- [x] Verify Sentry alert delivery and the Vercel runtime/error dashboard without exposing protected data. On 2026-08-19 Vercel reported no error/fatal logs for READY deployment `dpl_AYVFwDEq7SH8F7d4T242LSkBfYCm`; Sentry displayed the PII-free synthetic acceptance issue, its enabled email alert recorded one trigger, and Chang Yang confirmed recipient-side delivery.

## Synthetic Production-origin pilot

- [ ] Confirm the isolated target contains synthetic `example.test` data only and no real member data.
- [ ] After every Stage 22 gate passes, set `LOCAL801_SYNTHETIC_PRODUCTION_PILOT_ENABLED=1` and `LOCAL801_SYNTHETIC_DATA_ONLY=1`, while keeping `LOCAL801_PRODUCTION_LAUNCH_ENABLED=0` and all authoritative import execution switches off.
- [ ] Run the approved three-day pilot. Day 1 includes administrators and CAT leadership; after one successful day add Report Viewers. Test all seven roles, MFA, policy acknowledgement, session revocation, scanner paths, browser push, responsive desktop/mobile-web critical flows, and support escalation.
- [ ] Close every launch blocker. Then set both synthetic-pilot flags back to `0`. Their enabled state is a coded final-launch blocker.

## Final move to Production

- [x] Record the independent security approval and set `LOCAL801_SECURITY_REVIEW_APPROVED=1` plus a non-secret `LOCAL801_PRODUCTION_SECURITY_REVIEW_ID`. Completed 2026-08-19 using review `L801-SEC-2026-08-19-SY-01` for immutable commit `48e801a4cfffa95954061486961660799b7c14b0`; the external penetration-test report reference is `cat.chang.io-TESTANDREVIEWv1`.
- [x] Set `LOCAL801_DATABASE_PII_PROTECTION_ENABLED=1` only after the target database proves protected-only state. Set on 2026-08-19 after protected bootstrap and database verification passed.
- [x] Set `LOCAL801_BACKUP_RESTORE_VERIFIED=1` only after both documented recovery exercises pass. The isolated Neon recovery and encrypted R2 recovery/reconciliation exercises passed on 2026-08-19, after which the Sensitive Production-only flag was set to `1`. Authentication, synthetic pilot, authoritative imports, real data, and final launch remain disabled.
- [x] Inspect the safe `vercel-production-build-preflight` report from the final Production deployment. Because Vercel Sensitive values cannot be exported after creation, every Production build runs the preflight inside Vercel after compilation without changing launch flags or failing an intentionally locked deployment. Redeployment `dpl_62UR9orZnJa6DJcVkcx75CzBApkc` of reviewed commit `48e801a4cfffa95954061486961660799b7c14b0` reported `ready: true`, database `ok`, storage `ok`, and no blocker codes on 2026-08-19. The live site remained launch-disabled with no authentication providers exposed.
- [ ] Chang Yang gives the explicit written production and real-data authorization.
- [ ] Set `LOCAL801_PRODUCTION_LAUNCH_ENABLED=1` last, deploy the immutable approved SHA, load only the approved initial roster, and reconcile it to the authoritative source.
- [ ] After the launch flag deployment, run `npm run security:production-readiness` again against the active environment and preserve the safe result in the change record.
