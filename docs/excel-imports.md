# Excel And CSV Imports

Imports are uploaded to private Local 801 storage, malware scanned, validated, mapped, reviewed, and approved before changing official data.

The workflow rejects legacy `.xls` files by default, validates MIME and size, removes blank template rows, converts Excel serial dates, filters multi-local files to Local `0801`, detects duplicates, neutralizes spreadsheet formulas, and preserves source provenance.
