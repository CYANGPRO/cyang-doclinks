# Member Matching

Work email is the primary matching key when present and unique. Work-email changes require review because no immutable employee or membership ID was identified.

Ambiguous rows produce match candidates and do not update approved roster data until resolved. Missing rows in an import are not automatic drops.
